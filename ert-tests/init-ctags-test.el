;;; init-ctags --- emacs selftest
;;;
;;; Test for ctags integration
;;;
;;; Commentary:

;;; Code:
;;; noninteractive

;; (ert-deftest has-ctags nil
;;   "Emacs should have access to ctags helper"
;;   (should (featurep 'init-ctags))
;;   (should (featurep 'ctags-update))
;;   (should (featurep 'etags-select))
;;   (should (fboundp 'ctags-update))
;;   (should (fboundp 'etags-select-find-tag))
;;   (should (eq (global-key-binding "\C-cE") 'ctags-update))
;;   (should (eq (global-key-binding "\M-.") 'etags-select-find-tag)))

;;
;; Emacs
;;
;; Local Variables:
;; indent-tabs-mode: nil
;; coding: utf-8
;; End:
;;

;;; init-ctags.el ends here
